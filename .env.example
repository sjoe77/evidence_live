# Flight SQL Configuration
FLIGHT_SQL_ENDPOINT=http://localhost:8080/api/sql

# Mock Mode (set to 'true' to enable mock responses for all Flight SQL queries)
FLIGHT_SQL_MOCK=false

# Development
NODE_ENV=development
DEBUG=evidence:*

# Optional: Authentication (if needed)
# FLIGHT_SQL_AUTH=Bearer your-token-here